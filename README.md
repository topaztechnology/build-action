# Topaz Github build action

A generic build action for Topaz images, which supports a multiarch build (amd64 & arm64) using QEMU, and publishes to 
the Topaz accounts on Docker Hub & Github Container Repository.
